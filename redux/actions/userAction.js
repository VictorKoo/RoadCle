export const newUser = (user) => ({
  type: 'NEW_USER',
  user,
});
export const updateUser = (user) => ({
  type: 'UPDATE_USER',
  user,
});
// export const initUser = () => ({
//   type: 'INIT_USER',
// });
